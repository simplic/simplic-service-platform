<Window x:Class="Simplic.ServicePlatform.UI.Views.AddCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Simplic.ServicePlatform.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300" Background="White" Width="300" Height="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Create new Card" Grid.ColumnSpan="2" TextAlignment="Center" FontWeight="Bold" FontSize="20"/>
        <Label Content="Service Name: " Grid.Row="1" Grid.Column="0" />
        <TextBox Text="{Binding CardName}" Grid.Row="1" Grid.Column="1" MinWidth="200" HorizontalAlignment="Center"/>
    </Grid>
</Window>
