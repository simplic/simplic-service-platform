<Window x:Class="Simplic.ServicePlatform.UI.ModuleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:Simplic.ServicePlatform.UI"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding ParsedName}"
                   Foreground="DeepSkyBlue"
                   FontSize="30"/>
        <Border Grid.Row="1" CornerRadius="10" Height="auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <WrapPanel Orientation="Vertical" MinWidth="400">
                    <StackPanel>
                        <Label Content="Name"/>
                        <telerik:RadWatermarkTextBox WatermarkContent="Name"
                                                     Text="{Binding Name}"/>
                    </StackPanel>

                    <StackPanel>
                        <Label Content="Description"/>
                        <telerik:RadWatermarkTextBox WatermarkContent="Description"
                                                     Text="{Binding Description}"
                                                     Height="100"/>
                    </StackPanel>
                    
                    <StackPanel>
                        <Label Content="Assembly"/>
                        <telerik:RadWatermarkTextBox WatermarkContent="some.assembly.example"
                                                     Text="{Binding Assembly}"/>
                    </StackPanel>
                </WrapPanel>

                <StackPanel Grid.Row="1">
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Enable Auto Start"/>
                        <telerik:RadToggleButton Content="{Binding EnableAutoStartText, UpdateSourceTrigger=PropertyChanged}"
                                                 IsEnabled="{Binding EnableAutoStart}"
                                                 Height="40" Width="40"/>

                    </StackPanel>
                    <telerik:RadGridView ItemsSource="{Binding Requires}"/>
                    <telerik:RadGridView ItemsSource="{Binding ConfigurationDefinition}">
                        <telerik:RadGridView.Columns>
                            <telerik:GridViewDataColumn DataMemberBinding="{Binding Name}"/>
                            <telerik:GridViewDataColumn DataMemberBinding="{Binding Default}"/>
                        </telerik:RadGridView.Columns>
                    </telerik:RadGridView>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
